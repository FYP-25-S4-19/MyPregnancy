# Yes, I meant to commit this

APP_ENV=dev # Set to 'prod' during prod

# Obviously, use something different during prod
# This is just to save time for local devs
SECRET_KEY=BOFlVC5OMOAyV7qxoQhhlTmUoR37PPy6

POSTGRES_DB=fyp_db
POSTGRES_USER=fyp_user
POSTGRES_PASSWORD=fyp_password
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
SYNC_DATABASE_URL=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_SERVER}:${POSTGRES_PORT}/${POSTGRES_DB} # For use by synchronous SQLALchemy, Alembic
ASYNC_DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_SERVER}:${POSTGRES_PORT}/${POSTGRES_DB} # For use by asynchronous SQLAlchemy, tests

AWS_ACCESS_KEY_ID=test # For dev only. Will be 'none' during prod
AWS_SECRET_ACCESS_KEY=test # For dev only. Will be 'none' during prod
AWS_ENDPOINT_URL=http://localstack:4566 # For dev only. Will be 'none' during prod
LOCALSTACK_ENDPOINT_URL=http://localhost:4566 # For dev only. Will be 'none' during prod

S3_BUCKET_NAME=mypregnancy-bucket
S3_BUCKET_REGION=ap-southeast-1

JWT_EXP_SECONDS=3600

# Nullable per the settings object
# Will not be used during things like CI runner tests
STREAM_API_KEY=your_api_key_here
STREAM_API_SECRET=your_api_secret_here

# Google Cloud Vision (used by merchants to scan product images)
# Put the service account JSON in backend/secrets/gcp-sa.json (do not commit it)
VISION_ENABLED=true
GOOGLE_APPLICATION_CREDENTIALS=./secrets/gcp-sa.json

DOCTOR_CARD_PRESIGNED_URL_EXP_SECONDS=900 # 15 minutes
# Use this in production to protect the "/docs", "/redoc", and "/openapi.json" routes behind credentials
# Leave empty (or omit/delete)
# DOCS_USERNAME=
# DOCS_PASSWORD=
