// import { Channel, Thread } from "stream-chat";
// import { create } from "zustand";

// type ChatState = {
//   channel: Channel | null;
//   thread: Thread | null;

//   setChannel: (channel: Channel | null) => void;
//   setThread: (thread: Thread | null) => void;
//   clear: () => void;
// };

// const useChatStore = create<ChatState>((set) => ({
//   channel: null,
//   thread: null,

//   setChannel: (channel) => set({ channel: channel }),
//   setThread: (thread) => set({ thread: thread }),
//   clear: () => set({ channel: null, thread: null }),
// }));

// export default useChatStore;
